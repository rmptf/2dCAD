// Isosceles Triangle

// Find Height of Isosceles Triangle. (Given base and side lenths.)
// Formula
// h² = a² - (b/2)²
// Algorithm
let side = 15
let base = 6.6
let height = Math.sqrt((Math.pow(side, 2)) - (Math.pow((base / 2), 2)))
console.log(height)

